# Rust
/target/
**/*.rs.bk

# V8 snapshot / build artifacts
*.bin

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Logs / runtime
*.log
cookie-store.json

# Local npx skills install artifacts / session state
.agents/
.claude/
skills-lock.json

# Local publisher tooling binary
mcp-publisher

# localStorage persistence test artifact (runtime state, must never rsync to hosted instance)
/localStorage/
