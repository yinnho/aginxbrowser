{
  "args": ["title", "content", "tags", "images"],
  "vars": {
    "args_json": "{\"title\": \"测试标题（请换成真的）\", \"content\": \"第一段\\n\\n第二段\", \"tags\": [\"aginxbrowser\"], \"images\": [{\"name\": \"1.png\", \"content_type\": \"image/png\", \"content_base64\": \"CHANGE_ME\"}]}"
  },
  "create": {
    "url": "https://creator.xiaohongshu.com/publish/publish?source=official",
    "width": 1440,
    "height": 900
  },
  "steps": [
    {
      "op": "navigate",
      "args": {
        "url": "https://creator.xiaohongshu.com/publish/publish?source=official"
      },
      "note": "session_id reuse skips the whole create block (including its navigation) — without this the flow would run on whatever page the session was left on"
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 1.5: XHR auto-signing patch — the page bundles its signer as window._webmsxyw but never wires it up in this engine (mnsv2 installer chain gap), so every signed API call goes out bare and 406s, which the page misreads as session death and bounces to /login. Signer presence is checked at send time, so installing early is safe\n(() => {\n  if (window.__xs_installed) return 'already';\n  window.__xs_installed = true;\n  const O = XMLHttpRequest.prototype.open, S = XMLHttpRequest.prototype.setRequestHeader, D = XMLHttpRequest.prototype.send;\n  XMLHttpRequest.prototype.open = function(m, u) {\n    this.__xs_u = String(u); this.__xs_m = String(m || 'GET').toUpperCase();\n    return O.apply(this, arguments);\n  };\n  XMLHttpRequest.prototype.setRequestHeader = function(k, v) {\n    if (String(k).toLowerCase() === 'x-s') this.__xs_has = true;\n    return S.apply(this, arguments);\n  };\n  XMLHttpRequest.prototype.send = function(b) {\n    try {\n      if (typeof window._webmsxyw === 'function' && !this.__xs_has && this.__xs_u &&\n          this.__xs_u.indexOf('xiaohongshu.com') !== -1 && !(b instanceof FormData)) {\n        const path = this.__xs_u.replace(/^https?:\\/\\/[^\\/]+/, '');\n        let data = b;\n        if (this.__xs_m === 'POST' && typeof b === 'string') { try { data = JSON.parse(b); } catch (e) {} }\n        const sg = window._webmsxyw(path, data);\n        if (sg && sg['X-s']) {\n          S.call(this, 'X-s', String(sg['X-s']));\n          S.call(this, 'X-t', String(sg['X-t']));\n          S.call(this, 'X-Mns', 'unload');\n          window.__xs_signed = (window.__xs_signed || 0) + 1;\n        }\n      }\n    } catch (e) {}\n    return D.apply(this, arguments);\n  };\n  return 'patched';\n})()"
      },
      "save": "sign_patch"
    },
    {
      "op": "wait",
      "args": {
        "predicate": "!!document.querySelector('div.upload-content') || location.href.includes('/login')",
        "timeout_ms": 20000
      }
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 2: login-wall telemetry — no expect here: the 401 redirect ping-pongs (login bounces back via lastUrl, then 401 again), so a point-in-time URL check can pass mid-pong; the hard gate is the next wait's positive signal\nJSON.stringify({url: location.href, need_login: location.href.includes('/login'), title: document.title})"
      },
      "save": "wall"
    },
    {
      "op": "wait",
      "args": {
        "selector": "div.upload-content",
        "timeout_ms": 20000
      }
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 4: drop the blocking popover (upstream's always-works last resort) and activate the 上传图文 tab. Failure paths throw — an eval's return value is telemetry only, it does not stop the flow\n(() => {\n  const pop = document.querySelector('div.d-popover');\n  if (pop) pop.remove();\n  const tabs = [...document.querySelectorAll('div.creator-tab')].map(t => (t.textContent || '').trim());\n  const tab = [...document.querySelectorAll('div.creator-tab')].find(t => (t.textContent || '').trim() === '上传图文');\n  if (!tab) throw new Error('no 上传图文 tab, tabs=' + JSON.stringify(tabs));\n  tab.click();\n  return JSON.stringify({ok: true, tabs});\n})()"
      },
      "expect": {
        "selector": "input[type=file]"
      },
      "save": "tab"
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 5: upload images — data: URL fetch decodes base64 (atob+spread blows the stack on big files), File[] lands in input.files, manual input+change dispatch (JS assignment fires neither by itself). Real-send caveat: a single-File assignment silently no-ops in the engine today (files.length reads back 0); the proven real-page path is the HTTP face POST /session/:id/files — kept here because the flow executor runs in-page and the array face is what the mock dogfood verified\n(async () => {\n  const ARGS = {{args_json}};\n  if (!ARGS.images || !ARGS.images.length) throw new Error('no images');\n  window.__xhs_n = ARGS.images.length;\n  const inp = document.querySelector('input.upload-input') || document.querySelector('input[type=file]');\n  if (!inp) throw new Error('no file input');\n  const files = [];\n  for (const im of ARGS.images) {\n    const type = im.content_type || 'image/png';\n    const blob = await (await fetch('data:' + type + ';base64,' + im.content_base64)).blob();\n    files.push(new File([blob], im.name || 'image.png', {type}));\n  }\n  inp.files = files;\n  inp.dispatchEvent(new Event('input', {bubbles: true}));\n  inp.dispatchEvent(new Event('change', {bubbles: true}));\n  if (inp.files.length < files.length) throw new Error('input.files assignment no-op, len=' + inp.files.length);\n  return JSON.stringify({ok: true, n: files.length});\n})()"
      },
      "save": "upload"
    },
    {
      "op": "wait",
      "args": {
        "predicate": "document.querySelectorAll('.img-preview-area .pr').length >= (window.__xhs_n || 1)",
        "timeout_ms": 60000
      }
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 7: title via native setter + events (React value-tracker bypass), body via tiptap paragraph HTML + input event, then upstream's length guards — over-limit throws so the receipt names the real cause instead of a downstream submit-disabled\n(async () => {\n  const ARGS = {{args_json}};\n  const esc = (s) => String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');\n  const out = {};\n  const ti = document.querySelector('div.d-input input') || document.querySelector('input[placeholder*=\"填写标题\"]');\n  if (!ti) throw new Error('no title input');\n  Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set.call(ti, ARGS.title);\n  ti.dispatchEvent(new Event('input', {bubbles: true}));\n  ti.dispatchEvent(new Event('change', {bubbles: true}));\n  out.title_len = ti.value.length;\n  out.title_ok = ti.value === String(ARGS.title);\n  const ce = document.querySelector('div[role=\"textbox\"][contenteditable=\"true\"]') || document.querySelector('div.tiptap[contenteditable=\"true\"]') || document.querySelector('div.ql-editor');\n  if (!ce) throw new Error('no content editor, title_len=' + out.title_len);\n  const paras = String(ARGS.content || '').split(/\\n+/).filter(Boolean).map(esc);\n  const tags = (ARGS.tags || []).map(t => '#' + String(t).replace(/^#/, ''));\n  if (tags.length) paras.push(esc(tags.join(' ')));\n  ce.innerHTML = paras.map(p => '<p>' + p + '</p>').join('<p><br></p>');\n  ce.dispatchEvent(new Event('input', {bubbles: true}));\n  out.content_text_len = (ce.innerText || '').length;\n  out.title_limit = (document.querySelector('div.title-container div.max_suffix') || {textContent: null}).textContent;\n  out.content_limit = (document.querySelector('div.edit-container div.length-error') || {textContent: null}).textContent;\n  ti.click(); // upstream stability quirk: re-focus title after body fill\n  if (!out.title_ok) throw new Error('title readback mismatch: ' + JSON.stringify(out));\n  if (out.title_limit || out.content_limit) throw new Error('length over: title=' + out.title_limit + ' content=' + out.content_limit);\n  return JSON.stringify(out);\n})()"
      },
      "save": "form"
    },
    {
      "op": "wait",
      "args": {
        "predicate": "(function(){var ws=document.querySelectorAll('xhs-publish-btn');for(var i=0;i<ws.length;i++){var w=ws[i];if(w.getAttribute('is-publish')==='false')continue;if(w.getAttribute('submit-disabled')!=='true')return true}var bs=document.querySelectorAll('.publish-page-publish-btn button.bg-red');for(var j=0;j<bs.length;j++){var b=bs[j];if(!b.disabled&&b.getAttribute('aria-disabled')!=='true')return true}return false})()",
        "timeout_ms": 10000
      }
    },
    {
      "op": "eval",
      "args": {
        "script": "// xhs-post step 9: publish — the widget face never upgrades in this engine (customElements.define registers but upgrades nothing), so the inner shadow button does not exist to click; but the widget's own contract is a bubbling 'publish' CustomEvent on the host (its constructor wires exactly that), and the parent's Vue listener rides on addEventListener, which never needed the upgrade. Real-send verified 2026-09-20: dispatch → POST edith /web_api/sns/v2/note → 200 success:true → /publish/success\n(() => {\n  for (const w of document.querySelectorAll('xhs-publish-btn')) {\n    if (w.getAttribute('is-publish') === 'false') continue; // decorative clones\n    if (w.getAttribute('submit-disabled') === 'true') continue;\n    w.dispatchEvent(new CustomEvent('publish', {bubbles: true, composed: true}));\n    return JSON.stringify({ok: true, face: 'widget-event'});\n  }\n  for (const b of document.querySelectorAll('.publish-page-publish-btn button.bg-red')) {\n    if (b.disabled || b.getAttribute('aria-disabled') === 'true') continue;\n    b.click();\n    return JSON.stringify({ok: true, face: 'legacy-click'});\n  }\n  throw new Error('no publishable face (wait passed but re-disabled?)');\n})()"
      },
      "save": "publish"
    },
    {
      "op": "wait",
      "args": {
        "predicate": "location.href.includes('/publish/success') && !location.href.includes('/login')",
        "timeout_ms": 15000
      },
      "save": "verify",
      "note": "positive assertion: the success page, explicitly excluding /login — the old negative form (!includes('/publish/publish')) counted the 401 login bounce as success (false green, real-send 2026-09-19)"
    }
  ]
}
